let l = 1

for(l=0;l<=100;l++)
    {
        // Divisible by 5 and 7
        if(l%5==0 && l%7==0)
            {
                console.log(l)
            }
    
    
    }

    // for(initialization, conditional check, updation)
    // while(condition)
    // do {  } while(condition) 

    // if else --- Conditional Check
    // switch
